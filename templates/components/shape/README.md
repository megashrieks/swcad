# {{name}}

| File | Purpose |
| --- | --- |
| `component.json` | Manifest: id, name, version, params, default size. |
| `shape.svg` | The drawing. Its `viewBox` sets the default size; ids link elements to annotations. |
| `annotations.json` | What each element *means*: port, label, handle, fill slot, anchor, hit area. |
| `script.js` | Optional behaviour, run in a sandbox. |
